package org.xtraktor.preprocess.interpolation;

import org.xtraktor.LongPoint;
import org.xtraktor.ShortPoint;
import org.xtraktor.preprocess.PointsInterpolation;

import java.util.List;

public class SimplePointsInterpolation implements PointsInterpolation {
    @Override
    public List<ShortPoint> runInterpolation(LongPoint point) {
        return null;
    }
}