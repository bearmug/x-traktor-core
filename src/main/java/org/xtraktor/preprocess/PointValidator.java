package org.xtraktor.preprocess;

import org.xtraktor.LongPoint;

public class PointValidator {
    public boolean isValid(LongPoint point) {
        return point.getNextPoint() != null
                && ;
    }
}
